apiVersion: v1
kind: Service
metadata:
  name: devops-project-service
  namespace: prod
spec:
  selector:
    app: devops-project
  type: ClusterIP # Change to LoadBalancer if using cloud
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP