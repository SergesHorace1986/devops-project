apiVersion: v1
kind: ConfigMap
metadata:
  name: devops-project-config
  namespace: dev
  labels:
    app: devops-project
    env: dev
data:
  NODE_ENV: "development"
  LOG_LEVEL: "debug"
  PORT: "3000"
  APP_ENV: "dev"
