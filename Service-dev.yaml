apiVersion: v1
kind: Service
metadata:
  name: devops-project-service
  namespace: dev
spec:
  selector:
    app: devops-project
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP